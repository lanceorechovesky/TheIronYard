%nav.navbar.navbar-default{:role => "navigation"}
  .container-fluid
    / Brand and toggle get grouped for better mobile display
    .navbar-header
      %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => "/"} Home
    / Collect the nav links, forms, and other content for toggling
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li.active
          - if user_signed_in?
            = content_tag(:a, current_user.email)
          - else
            = link_to "Log In", new_user_session_path
        - if user_signed_in?
          %li
            = link_to "Hospitals", hospitals_path
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              Edit
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li
                = link_to "#{@hospital.name}'s Data", edit_hospital_path(@hospital)
              %li.divider
                %li.dropdown-header{:role => "presentation"} Remove Doctors                 
                - @hospital.doctors.each do |doc|
                  %li
                    = link_to "#{doc.name}", destroy_doctor_hospital_path(@hospital, doctor_id: doc.id), method: :delete
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              Patients
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li
                = link_to "All patients", hospital_patients_path(@hospital)
              %li.divider
              %li.dropdown-header{:role => "presentation"} Patient List 
              - @hospital.patients.each do |pat|
                %li
                  = link_to "#{pat.last_name}", hospital_patient_path(@hospital, pat)
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          %li.dropdown
            %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
              Account
              %span.caret
            %ul.dropdown-menu{:role => "menu"}
              %li
                = link_to "Edit", edit_user_registration_path
              %li.divider
              %li
                = link_to "Log Out", destroy_user_session_path, method: :delete
    / /.navbar-collapse
  / /.container-fluid




.row.text-center
  .col-md-12
    .panel.panel-success
      .panel-heading
        %h1.panel-title 
          = link_to "#{@hospital.name}'s Data", edit_hospital_path(@hospital)
      .panel-body
        %ul
        %strong
          = "name"
        %br/
        = @hospital.name
        %br/
        %br/
        %strong
          = "address"
        %br/
        = @hospital.address
        %br/
        %br/
        %strong
          = "Phone Number"
        %br/
        = @hospital.phone_number
        %br/
        %br/
        %strong
          = "Doctors on call"
        %br/
        - @hospital.doctors.each do |doc|
          - if doc.name
            = doc.name
            = link_to "*remove*", destroy_doctor_hospital_path(@hospital, doctor_id: doc.id), method: :delete
            %br/




















